name: Deploy Hugo Markdown Docs

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: latest
          extended: true

      - name: Create temporary Hugo site
        run: |
          mkdir -p /tmp/hugo/content
          mkdir -p /tmp/hugo/static

          # 複製 Markdown 到 Hugo content
          rsync -av \
            --include='*/' \
            --include='*.md' \
            --exclude='*' \
            ./ /tmp/hugo/content/

          # 複製文件附件到 Hugo static
          rsync -av \
            --include='*/' \
            --include='*.pdf' \
            --include='*.png' \
            --include='*.jpg' \
            --include='*.jpeg' \
            --include='*.gif' \
            --include='*.svg' \
            --include='*.webp' \
            --exclude='*' \
            ./ /tmp/hugo/static/

          # Hugo 首頁使用 _index.md
          if [ -f /tmp/hugo/content/index.md ]; then
            mv /tmp/hugo/content/index.md /tmp/hugo/content/_index.md
          fi

          cp ./hugo.toml /tmp/hugo/hugo.toml
          rsync -av ./layouts/ /tmp/hugo/layouts/

          if [ -d ./static ]; then
            rsync -av ./static/ /tmp/hugo/static/
          fi

          if [ -d ./assets ]; then
            rsync -av ./assets/ /tmp/hugo/assets/
          fi

      - name: Build Hugo site
        run: |
          hugo --source /tmp/hugo --destination /tmp/public --minify --baseURL "${{ steps.pages.outputs.base_url }}/"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: /tmp/public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
